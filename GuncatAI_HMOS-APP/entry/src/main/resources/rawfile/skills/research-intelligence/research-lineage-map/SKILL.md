---
name: research-lineage-map
description: 绘制研究领域或技术主题的谱系脉络与历史演进图，可视化思想的演化路径，展示早期工作中的技术难题如何被后续研究逐步解决。当用户想了解某个主题的发展轨迹、某个模型或技术的"家族树"（family tree）、某条研究线索在多年间的演进路线、技术迭代脉络、论文/模型谱系，或询问"X 是如何一步步发展来的""X 解决了前人的什么问题""梳理 X 的发展历史"时触发。产出为嵌入 Mermaid 图表的 Markdown 文件（演进图 + 节点明细表 + 阶段叙事），可直接粘贴至 Mermaid 查看器或提交至代码仓库。
agent_created: true
---

# Research Lineage Map 研究谱系演进图

## Overview

为研究主题、模型家族或技术线索绘制"家谱式"演进图，核心是把**思想的演化路径**可视化：每个关键工作解决了什么问题、又被谁如何改进。最终交付一个嵌入 Mermaid 图表的独立 Markdown 文件。

## 核心设计原则

1. **"问题 → 解决"叙事是灵魂**：每条演进边必须回答"后来者解决了前人的什么问题"，而不是只画引用关系。
2. **年份是硬约束**：每个节点必须标注发表/发布年份，且关键节点的年份与血缘关系经联网核实。
3. **少而准优于多而错**：精选 8–20 个真正有承继关系的关键节点，拒绝堆砌。
4. **事实冲突以检索为准**：内部知识与联网结果冲突时，采用检索结果并在参考资料区留痕。

## 工作流程

### 第 1 步：明确范围与粒度

确认主题边界、时间跨度与节点粒度（论文级 / 模型版本级 / 范式级）。主题过大时（如"整个 NLP 史"），先收窄，或采用"总览图 + 分支子图"的分层画法。用户意图明确时直接判断，不必反复追问。

### 第 2 步：起草谱系节点清单

基于内部知识列出候选节点，每个节点记录：名称、年份、核心贡献、**解决了什么问题**、改进自谁、被谁改进。此清单是后续绘图与写表格的同一份数据源。

### 第 3 步：联网核实关键史实

用 `search_web` / `web_fetch` 核实每个节点的：发表或发布年份、名称拼写、承继关系（谁明确提出要解决谁的什么问题）。优先来源：arXiv 论文页、官方博客、机构公告、经典综述。核实失败的节点在明细表中标注"（年份待考）"，不得编造。

### 第 4 步：设计 Mermaid 谱系图

先阅读 `references/mermaid-patterns.md`（含语法模式与高频坑）。要点：

- 主图用 `flowchart`，按年代或范式划分 `subgraph` 泳道，用 `classDef` 着色区分开创性工作 / 增量改进 / 旁支。
- 演进边用带文字的箭头（`-->|解决了什么问题|`），旁支或弱化关系用虚线。
- 节点超过约 15 个或明显多分支时，拆为总览图 + 若干分支子图。
- 可选：文末附 `timeline` 简版时间线作为速览。

### 第 5 步：撰写 Markdown 文件

严格按 `references/output-template.md` 的结构产出：标题与一句话概览 → 主演进图 → 关键节点明细表（年份 / 贡献 / 解决的问题 / 来源）→ 阶段叙事（按年代分段，讲清"问题 → 解决"链条）→ 可选时间线 → 参考资料。文件语言跟随用户语言。

### 第 6 步：校验与交付

本环境无终端/脚本，不运行任何校验命令；改为人工核对每个 Mermaid 代码块（语法、引号、`-->|标签|` 写法、subgraph 闭合），修复明显报错后再交付。

核对通过后，用 `write_file` 交付 `.md` 文件并告知文件路径，提示用户：GitHub / GitLab / VS Code / Typora / Obsidian 及 mermaid.live 均可直接渲染。可选定制：节点明细表另用 `load_skill("xlsx")` + `write_xlsx` 输出（节点/年份/贡献/解决的问题/改进自/被谁改进/来源），便于筛选与补录；需要静态图时用 `write_svg`（`svg` 技能）生成 SVG 版演进图作为附件。

## Resources

- `references/mermaid-patterns.md` — 谱系图专用 Mermaid 语法模式、配色语义与常见语法坑（第 4 步前必读）
- `references/output-template.md` — 输出 Markdown 的完整模板与填充示例（第 5 步前必读）

---

> 本项目适配（不替代原文）：`scripts/validate_mermaid.py` 在本环境不可用已移除，校验改为人工核对；联网用 `search_web`/`web_fetch`，交付用 `write_file`，节点明细表可用 `load_skill("xlsx")` + `write_xlsx`，静态演进图可用 `write_svg`。原文的谱系叙事方法、Mermaid 模式与输出模板全部保留。
